{{#each products as |product|}}

  <ul class="product-form" data-id="{{product.id}}">
    <div>Id: {{product.id}}</div>
    <div>Name: {{product.name}}</div>
    <div>Description: {{product.description}}</div>
    {{!-- <div>Stock: {{product.stock}}</div> --}}
    {{!-- <div class="add-resource-product">
      <form class="add-product-form">
        <fieldset>
        <input class="add-to-orders-button" type ="submit" name="submit" data-id="{{product.id}}" value="Add Product"></input>
        <input type="text" name='order[product_id]' class="clear-input" placeholder='id'>
        <input class="id-value" name="order[product_id]" value="{{product.id}}" type="hidden"></input>
        </fieldset>
      </form>
    </div> --}}
    </ul>
{{/each}}
